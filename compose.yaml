version: '3.8'

services:
  cisgo-web:
    build: .
    ports:
      - '8000:80'

  cisgo-firebase-admin:
    build:
      context: ./node

  cisgo-spaces:
    build:
      context: ./python
    command: python ./index.py
